# Handoff: push to the team repository

The completed work is on the `phase-0-2-backend` branch of the shared fork:

```text
https://github.com/riyadoshi014-commits/Shipyard-Pine-View
```

It is also available for review in pull request #1 against `soniccombinator/Shipyard-Pine-View`.

## For a teammate with write access

1. Open pull request #1 in GitHub and review the changes.
2. Merge the pull request into the team repository's `main` branch using your own GitHub account, or check out the branch locally and push it with your credentials.
3. Do not commit `.env.local` or any passwords, keys, database URLs, or email credentials.

The branch contains the mentor dashboard, protected Phase 0/2 backend work, and the scheduled check-in email implementation.
